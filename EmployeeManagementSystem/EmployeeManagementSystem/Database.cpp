#include "Database.h"

/*
template<typename record>
vector<record> VectorDatabase<record>::del(string const& column, string const& keyword) {
	vector<record> deletedRecs;
	for (auto& r : database) {
		if (r.isMatch(column, keyword)) {
			database.erase(r);
			deletedRecs.push_back(r);
		}
	}

	return deletedRecs;
}
*/